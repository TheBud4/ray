# Diário de aprendizado pessoal — não viaja com o repo (design §9.2)
.claude/.local/

# Editor config pessoal
.vscode/

# Sessões do companheiro visual de brainstorming (superpowers)
.superpowers/

# Estado vivo entre sessões — gerido pela IA, não viaja com o repo
.claude/handoff.md

# Métricas de runtime do session-start hook (I5) — mesma regra que o ray
# grava em .gitignore de todo projeto scaffoldado (scaffold.go:190)
.claude/.ray-metrics/
