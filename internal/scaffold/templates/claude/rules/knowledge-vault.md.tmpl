# Regra: vault de conhecimento

Anotações de aprendizado, decisões e pesquisas que valem a pena preservar entre
sessões vão no vault da IA (`~/.ray/vault`, via MCP filesystem), não em
`docs/` (que é para humanos) nem soltas na conversa.
