# Regra: handoff

Antes de encerrar a sessão ou de uma limpeza de contexto, sobrescreva
`.claude/handoff.md` com o estado vivo: o que foi feito, o que falta, e
qualquer decisão pendente. É a única forma de continuidade entre sessões.
