---
id: NNN
titulo: Nome curto e verbal — ex "Criar tarefa com prazo"
status: rascunho   # rascunho | aprovada | implementada | descartada
depende_de: []     # ex [001, 003] — specs que precisam estar implementadas antes
criado_em: AAAA-MM-DD
---

<!--
COMO USAR:
- Copie este arquivo para specs/NNN-nome-da-feature.md
- Não repita nada do CLAUDE.md aqui (stack, convenções, gates). Se está copiando,
  está no lugar errado.
- status: aprovada só quando "Perguntas em aberto" estiver VAZIA.
- Apague estes comentários ao preencher.
-->

# NNN — Título

## Objetivo

<!-- 1-2 frases. O que o usuário consegue fazer depois disso que não conseguia
antes. Se você não consegue escrever em 2 frases, a feature é grande demais —
quebre em duas specs. -->

…

**Por que agora:** …

## Fora de escopo

<!-- Tão importante quanto o resto. Sem isso o modelo "ajuda" inventando features
e você paga na revisão. Liste o adjacente óbvio que ele NÃO deve construir. -->

- …
- …

## Requisitos funcionais

<!-- Um requisito é bom quando dá para escrever um teste que falha sem ele.
"Deve ser rápido" não é requisito. "p95 < 200ms com 10k linhas" é. -->

- **RF-01:** …
- **RF-02:** …

## Critérios de aceite

<!-- O CORAÇÃO. Cada CA vira ao menos um teste, nomeado `CA-NN: ...`.
Regra prática: para cada caminho feliz, escreva pelo menos um caminho triste.
O "Então" precisa ser OBSERVÁVEL de fora — status code, retorno, estado
persistido. Nunca "então o serviço chama o repositório". -->

### CA-01 — caminho feliz `(RF-01)`
- **Dado** … <!-- estado inicial concreto, com valores -->
- **Quando** …
- **Então** … <!-- resultado observável, com valores -->

### CA-02 — entrada inválida `(RF-01)`
- **Dado** …
- **Quando** …
- **Então** … <!-- erro esperado: status + código + mensagem -->

### CA-03 — borda `(RF-02)`
- **Dado** … <!-- lista vazia / limite máximo / concorrência -->
- **Quando** …
- **Então** …

### CA-04 — falha de dependência externa `(RF-02)`
- **Dado** … <!-- banco indisponível / API de terceiro retorna 500 -->
- **Quando** …
- **Então** … <!-- comportamento esperado, não "explode" -->

## Regras de negócio e invariantes

<!-- Coisas que precisam ser verdade SEMPRE, não só no fluxo acima. Costumam
virar os testes que pegam bug de verdade. É também onde moram as afirmações que
nenhum teste cobre — a revisão lê esta seção à mão. -->

- …
- …

## Contratos

<!-- Cole o real: tipos, JSON, schema, evento. Não descreva em prosa —
descrição vira alucinação. -->

```
…
```

**Formato de erro:**

```
…
```

## Dependências e impacto

<!-- O que já existe que essa feature toca. Evita que o modelo descubra na marra
e quebre algo. Esta lista é o que delimita o commit da spec. -->

- Toca: …
- Novo: …
- Quebra contrato existente? … <!-- se sim, qual e como migrar -->

## Estratégia de teste específica

<!-- Só o que FOGE do <test_strategy> do CLAUDE.md. Se não foge, apague a seção
inteira. -->

- …

## Perguntas em aberto

<!-- SEÇÃO MAIS IMPORTANTE DO DOCUMENTO.
Seja honesto aqui. Toda pergunta que ficar sem resposta é uma decisão que o
modelo vai tomar sozinho, em silêncio, e provavelmente errado.
A spec NÃO vira `status: aprovada` enquanto isto não estiver vazio. -->

- [ ] …
- [ ] …

## Decisões durante a implementação

<!-- Preencher DEPOIS, ao fechar a spec. Só o que divergiu do planejado e por
quê. É isto que impede a spec de virar ficção. -->

- …
